{
  "name": "trophway",
  "private": true,
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "mobile": "pnpm --filter @trophway/mobile"
  },
  "devDependencies": {
    "prettier": "^3.9.9",
    "turbo": "^2.11.3"
  }
}
